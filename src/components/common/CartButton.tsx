import { Button } from "../ui/button";

export const CartButton = () => {
  return (
    <div>
      <Button variant="default" className="cursor-pointer">
        Varukorg{" "}
      </Button>
    </div>
  );
};
